/** 
 * 배열 선언
 * 1. 변수명 : 타입[] = [값,,,]
 * 2. 변수명 : Array<타입> = [값...]
 * 
 * 
 * 
*/
const numbers1 : number[]=[1,2,3,4];
const numbers2 : Array<number> = [1,2,3,4];

//객체 배열
const idols(name:NamedCurve.string, group:string)[][
  {naem : '원영ㅇ', grpi[r]]